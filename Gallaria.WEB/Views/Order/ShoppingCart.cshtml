@model Gallaria.ApiClient.DTOs.OrderDto

@{
    ViewData["Title"] = "ShoppingCart";
}

<h1>ShoppingCart</h1>

<div>
    <h4>OrderDto</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Date)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Date)
        </dd>
        @foreach (var item in Model.OrderLineItems)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(modelItem => item.Art.Title)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(modelItem => item.Art.Title)
            </dd>
        }

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FinalPrice)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FinalPrice)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
